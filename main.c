#include "libft.h"
#include <stdio.h>
#include <string.h>

int main()
{
    int	cnt, n;

    n = -1;
	cnt = n <= 0;
	// cnt = 0;
	// if (n == 0)
	// 	return (1);
	// if (n < 0)
	// 	cnt++;
    printf("%d", cnt);
}